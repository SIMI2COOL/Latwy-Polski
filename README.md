# 🎓 New Learning App

A new learning application template based on the Polish Learning App architecture.

## 📋 Project Overview

This is a fresh clone of the Polish Learning App, ready to be customized for your new learning project.

## 🚀 Getting Started

### Prerequisites

- Node.js (v18 or higher)
- npm or yarn

### Installation

```bash
npm install
```

### Development

```bash
npm run dev
```

### Build

```bash
npm run build
```

### Lint

```bash
npm run lint
```

## 📁 Project Structure

- `src/components/` - React components
- `src/utils/` - Utility functions
- `src/data/` - Data files (vocabulary, etc.)
- `src/types/` - TypeScript type definitions
- `public/` - Static assets

## 🔧 Customization

1. Update `package.json` with your project name
2. Modify the vocabulary data in `src/data/`
3. Customize the UI components in `src/components/`
4. Update the logo and favicon in `src/components/common/Logo.tsx` and `public/favicon.svg`
5. Adjust the database schema in `src/utils/database.ts` if needed

## 📝 License

This project is ready for your customization!
